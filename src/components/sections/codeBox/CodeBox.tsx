import OpenFileBar from "./components/openFlieBar";

const CodeBox = () => {
  return (
    <section className="bg-slate-950 w-full h-screen pl-2">
      <OpenFileBar />
    </section>
  );
};

export default CodeBox;
